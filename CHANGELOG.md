# 版本记录

## 0.3.5

修复多账号实机检查暴露的尝试记录展示问题，并补齐队列关闭请求的 Windows 诊断回执。项目仍仅供学习自动化编排，真实多账号与全部目标游戏每日尚未完成验收。

- 尝试记录接口返回所属 Run 的账号身份，详情页只统计该账号、该 Run 的尝试，避免实际存在的人工门被显示为零次。
- 对已验证进程的关闭记录请求是否被接受、Windows 失败码和同一句柄的即时退出信号。关闭请求被接受不等于进程已经退出；原有关闭范围、等待上限和人工接管边界保持不变。
- 同步 Manager、平台包、WebGUI 与 Adapter Host 的产品版本为 `0.3.5`。保留原账号配置和历史，不重写完成证据。

实机 0.3.4 从“开始每日”建立了两个独立的鸣潮账号 Run，各有七个互不复用的 Todo；第一账号因已存在无可用窗口的客户端而暂停，未进入切换阶段。这一结果验证了配置和编排入口，不能作为账号切换或每日完成证据。

## 0.3.4

增加鸣潮同一客户端的多账号配置与隔离执行链。项目仅供学习自动化编排；真实多账号每日和全部目标游戏每日仍未完成实跑验收。

- 在鸣潮配置中管理账号别名、启用状态、顺序和官方登录页已记住账号的精确掩码标签。新账号 ID 由 Manager 生成；当前各账号共享每日步骤选择和 OK-WW 参数。
- 批次冻结每个游戏/账号目标及配置快照。每个账号有独立 Run、Todo、Attempt、证据和完成合同，续跑继承原账号，不能复用其他账号的完成记录。
- 迁移 Todo 唯一键以包含账号身份，同时保留旧默认账号的 Todo ID 和历史封存。已经创建执行 Run 的账号禁止更换登录标签；需要换号时新增账号并停用旧项。
- 在鸣潮每日步骤之前验证并选择已记住的账号。遇到登录、验证码、条款或缺失、歧义标签时暂停并保留现场，不自动填写凭据或同意条款。
- 页面展示各账号的独立步骤与运行状态；完成判断继续区分工具执行、证据复核和 `accepted_done`。
- 同步 Manager、平台包、WebGUI 与 Adapter Host 的产品版本为 `0.3.4`。依赖版本和 Adapter 协议仍按各自合同管理。

验证涵盖数据库迁移与历史保留、跨账号证据拒绝、冻结范围、排队阶段的标签保护、续跑和账号切换回放。离线测试或账号选择通过不代表游戏每日完成；公开仓库不含真实账号标签、UID、凭据、私人配置、日志或截图。StarRail 兼容载荷的公开构建限制仍见 README。

## 0.3.3

补齐队列最后一款游戏的资源回收，并修复升级过程中安全停机的状态版本竞争。

- 原生游戏的批次正常收尾使用冻结范围、本次安装绑定与经过进程身份验证的关闭能力；预先打开的最后一款游戏也会被关闭。Host 启动失败后的收尾沿用同一处理。
- 关闭期间遇到人工接管、取消或失去当前批次归属时保留客户端；未知身份与残留进程使清理失败，不把已发送关闭请求当作退出成功。
- 安全停止请求遇到 HTTP 412 时有限次重读快照，保持同一幂等键与请求内容。显式指定的状态版本不自动刷新，HTTP 409 和其他错误不盲目重试。
- 统一发布入口通过已测试的本机快照客户端申请停止现有 Manager，使生命周期修复能够升级旧安装版；不修改旧安装文件、不强杀 Host，也不取消新运行的批次。
- 独立平台验证将行为测试所需的三个固定脚本一起暂存，始终使用同一源码来源，避免暂存运行时找不到脚本或读到其他 checkout。
- 网页订阅启动前与正常收尾的逐游戏清理事件，让新回执及时触发快照刷新，不必等到整轮清理结束才看到进展。
- 同步产品版本为 `0.3.3`，更新操作说明。验证包括关闭归属、最后一款预先运行、人工/取消竞争、残留失败，以及安全停机的重试和来源路径行为。

本版本仍未完成目标七款游戏全部每日的实跑验收。升级保留本机配置与运行数据；逐游戏实际结果、奖励证据和人工门仍须在安装后检查。公开提交不包含私人配置或运行证据。

## 0.3.2

继续完善“开始每日”的本机编排与故障恢复，并整理公开学习用途的源码说明。当前仍为实验版本，7 款游戏全部每日的一键自动实跑验收尚未达成。

- 完善 Adapter 真实阶段事件、执行错误传播和逐 Todo 复核接口，保留未运行与待复核状态。
- 在启动游戏前遇到人工门时暂停后续调度并保留现场，通过显式释放接管和原运行恢复流程继续执行。
- 在启动每款游戏前，自动关闭同一冻结队列中的其他已配置客户端，包括预先打开的游戏；按路径与进程身份核验，有界退出并汇总残留，清理不完整时阻止后续启动。
- 将后台与定时入口的进程清理统一交给 Manager，取消时停止关闭和启动；修正残留进程被误报为已关闭的问题，记录实际内存观测值。
- 修复正式启动器识别和激活的窄缺口，保留稳定可见游戏窗口要求，加强取消时序、进程身份与启动器 UI 状态校验。
- 将上游动作结果与游戏内效果分开验收，无效果时保留证据并报告失败。
- 统一本机源码快照、构建、验证和安装入口；补齐历史产品快捷方式向单宿主入口的事务迁移与回滚验证。
- 整理公开 README、开发说明和第三方归属边界；账号、私有配置、日志与运行证据留在本机。
- 删除重复匹配界面文字、排版细节与本机历史迁移资料的测试，保留执行、取消、进程身份、配置及证据合同验证。
- 同步 Manager、平台包、WebGUI 和 Adapter Host 的产品版本元数据为 `0.3.2`，依赖与协议版本沿用各自合同。

相关单元、契约和快捷方式事务测试，以及部分正式启动器的受限只读检查已通过。完整发布仍应对同一份源码快照重新运行统一验证；这些结果不代表所有游戏每日已完成。

游戏和上游助手需独立安装及更新。StarRail 依赖未公开的兼容字节码载荷，公开仓库暂不能独立构建该 Adapter；其余 Adapter 也须满足本机依赖。登录、验证码、条款、付费或不明成本等情况仍需人工处理。升级前按本机维护流程保留运行配置，升级后从 WebGUI 检查勾选范围与待处理事项。
